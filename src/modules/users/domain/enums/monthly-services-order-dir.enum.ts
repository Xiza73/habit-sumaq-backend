/**
 * Direction of the monthly-services list ordering. Kept simple (ASC/DESC) so
 * the web's order toggle is a binary switch.
 */
export enum MonthlyServicesOrderDir {
  ASC = 'asc',
  DESC = 'desc',
}
